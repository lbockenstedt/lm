# Dev/test dependencies for the hub (not shipped to deployed installs).
# Install with: core/venv/bin/pip install -r core/requirements-dev.txt
# (installs core/requirements.txt too — dev env needs the runtime deps to
# import the modules under test).
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.23          # asyncio_mode=auto (pytest.ini); accepts 0.23+ incl 1.x (was pinned <1.0 but 1.x is installed)
pyflakes>=3.0                 # tests/test_routes_undefined_names.py — without it
                              # that guard silently importorskip()s and the
                              # NameError class of bug reaches production.
                              # ci.yml installs every requirements*.txt it finds,
                              # so listing it here is what turns the guard ON in CI.
